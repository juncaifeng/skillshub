# 2026-05-07

- 添加 agentskills/agentskills 仓库为子模块到 `./reference`
- 通读全部 agentskills 规范文档：specification.mdx、home.mdx、quickstart.mdx、best-practices.mdx、evaluating-skills.mdx、using-scripts.mdx、optimizing-descriptions.mdx、adding-skills-support.mdx
- 阅读 skills-ref 参考实现源码（models.py、parser.py、validator.py、cli.py、prompt.py）
- 基于用户提供的 buf + grpc-gateway 项目模板，提炼为用户级 skill `buf-grpc-gateway-scaffold`（~/.workbuddy/skills/），含完整目录结构、配置文件模板、Proto/Go/Makefile 模板、Gotchas 和最佳实践
- 按原子化原则新增三个 buf 子技能（用户级）：
  - `buf-add-service`（218行）：新增完整 gRPC 服务（proto→generate→实现→main.go 注册→编译验证）
  - `buf-add-rpc`（116行）：给已有服务追加 RPC 方法（proto→generate→实现→编译，不需改 main.go）
  - `buf-maintain`（145行）：日常维护合集（lint / breaking check / generate / mod update / build / clean），6 操作 checklist 驱动
