# 2026-05-11

- 新增 skill `buf-add-micro`（376行）— 独立微服务脚手架，与 `buf-add-service` 互补
  - buf-add-service: 同进程内新增 service（共用 go.mod/main.go/端口）
  - buf-add-micro: 新建独立微服务（独立 go.mod/main.go/端口，monorepo 部署拆分）
  - 9 步检查清单：目录骨架 → buf 配置 → proto → 生成 → 实现 → main.go → Makefile → 编译 → 测试
  - 含端口规划建议（:909N / :808N 递增规则）
