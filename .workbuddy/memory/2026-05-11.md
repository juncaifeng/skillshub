# 2026-05-11

- 新增 skill `buf-add-micro`（376行）— 独立微服务脚手架，与 `buf-add-service` 互补
- 架构重构（API Gateway 模式）：
  - `buf-grpc-gateway-scaffold` → v2.0 重构为独立 Gateway，新增"接入外部 gRPC 服务"章节（go.mod replace + gRPC client 代理 + 认证中间件）
  - 新增 `buf-add-grpc-svc`（400行）— 纯 gRPC 后端服务（无 HTTP gateway，proto 不含 HTTP 注解）
  - `buf-add-micro` 标注为 BFF 场景（仅特定用途），建议大部分场景用 buf-add-grpc-svc
  - `buf-add-service` 修复 Gotcha 中的 import 误导
- README 新增架构图 + 选型指南（决策树）
- 完整审查 5 个 skill，发现并修复 1 处冲突（add-service import gotcha）
